{
  "name": "general-stream-subtitle",
  "version": "0.3.0",
  "private": true,
  "description": "Lightweight multi-platform HLS/WebVTT subtitle translation middleware for Surge, Loon and Shadowrocket.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run build && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
