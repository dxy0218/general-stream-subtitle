{
  "name": "general-stream-subtitle",
  "version": "0.3.0",
  "private": true,
  "description": "Staged v0.4.0 release bootstrap.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/release-bootstrap.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
